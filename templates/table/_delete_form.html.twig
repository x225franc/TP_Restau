<form method="post" action="{{ path('table_delete', {'id': table.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ?');" style="display:inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ table.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>